import { Pressable, Text, View } from "react-native";

const CustomButton = () => {
  return (
    <Pressable>
      <Text> IOS Platform Button </Text>
    </Pressable>
  );
};

export default CustomButton;
